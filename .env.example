# ProServis - Varsayılan SMTP Ayarları (Örnek)
# Bu dosyayı .env olarak kopyalayın ve gerçek değerlerle doldurun

DEFAULT_SMTP_HOST=smtp.gmail.com
DEFAULT_SMTP_PORT=587
DEFAULT_SMTP_USER=your-email@gmail.com
DEFAULT_SMTP_PASSWORD=your-app-password-here
DEFAULT_SMTP_ENCRYPTION=TLS

# NOT: 
# - Gmail kullanıyorsanız, normal şifre yerine "App Password" kullanmalısınız
# - App Password oluşturmak için:
#   1. Google Hesap > Güvenlik > 2 Adımlı Doğrulama'yı aktif edin
#   2. Güvenlik > Uygulama Şifreleri'nden yeni şifre oluşturun
# - .env dosyası .gitignore'da olduğu için repository'ye commit edilmeyecektir
